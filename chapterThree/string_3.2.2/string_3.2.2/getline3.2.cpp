#include<string>
#include <iostream>
using namespace std;

/*一次读取一个单词（不含空格）
	while(cin>>line)
		cout << line << endl;
*/

/*
int main() {
	string line;
	while (getline(cin, line)) {
		if (!line.empty())
			cout << line << endl;
	}
	
	return 0;
}
*/
