
/*

#include <string>
#include <vector>
#include <iostream>
using namespace std;
int main() {
	vector<int> vt;
	int a;
	for (decltype(vt.size()) p = 0;; ++p) {
		cin >> a;
		vt.push_back(a);
		if (p >= 1)
			cout << vt[p - 1] + vt[p] << endl;
	}
	return 0;
}
*/
